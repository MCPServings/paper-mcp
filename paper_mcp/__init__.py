"""paper-mcp — a remotely callable MCP server for academic paper search.

Baseline ("套壳") milestone: a thin, source-agnostic wrapper exposing arXiv
search/fetch over the MCP streamable-HTTP transport. It is the skeleton the
value layers (semantic retrieval, extraction, synthesis) plug into later.
"""

__version__ = "0.1.0"
